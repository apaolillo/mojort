# mojort

Howto:

```bash
git clone git@gitlab.soft.vub.ac.be:perfsys/papers/mojort.git
cd mojort/
git submodule update --init --recursive
./configure.sh
. ./.venv/bin/activate
cd mojort/campaigns/
./latencytest.py
```
